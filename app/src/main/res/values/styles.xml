<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
    </style>

    <!-- Theme for the full screen layout -->
    <style name="AppBaseTheme" parent="Theme.MaterialComponents.NoActionBar" />

    <style name="preferenceTheme">@style/PreferenceThemeOverlay</style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Style of the photo ImageView -->
    <style name="PhotoImageView">
        <item name="android:layout_width">@dimen/photo_dimensions</item>
        <item name="android:layout_height">@dimen/photo_dimensions</item>
    </style>

    <!-- Style of the photo title TextView -->
    <style name="PhotoTitleTextView">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_margin">@dimen/margin</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <!-- Style of the subtitle TextView -->
    <style name="SubtitleTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Subtitle1
        </item>
    </style>

    <!-- Style of the photo description TextView -->
    <style name="PhotoDescriptionTextView">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:paddingLeft">@dimen/margin</item>
        <item name="android:paddingTop">@dimen/margin</item>
        <item name="android:paddingRight">@dimen/margin</item>
        <item name="android:textSize">@dimen/photo_description_text_size</item>
    </style>

    <!-- Style of the empty TextView -->
    <style name="EmptyTextViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <!-- Style of the loading indicator -->
    <style name="LoadingIndicatorStyle" parent="Widget.AppCompat.ProgressBar">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <!-- Style of the asteroid ImageView -->
    <style name="AsteroidImageViewStyle">
        <item name="android:layout_width">@dimen/image_dimensions</item>
        <item name="android:layout_height">@dimen/image_dimensions</item>
        <item name="android:layout_marginStart">@dimen/margin</item>
    </style>

    <!-- Style of the buttons -->
    <style name="ButtonStyle" parent="TextAppearance.AppCompat.Widget.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@color/colorTextIcons</item>
        <item name="android:paddingStart">@dimen/margin_list_item</item>
        <item name="android:paddingEnd">@dimen/margin_list_item</item>
    </style>

    <!-- Style of the asteroid labels TextView -->
    <style name="AsteroidLabelTextViewStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:textSize">@dimen/asteroid_text_size</item>
    </style>

    <!-- Style of the asteroid values TextView -->

    <!-- Style for the list item title of the observatory -->
    <style name="ObservatoryListItemTitleStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Subtitle1
        </item>
    </style>

    <!-- Style for the list item subtitle of the observatory -->
    <style name="ObservatoryListItemSubtitleStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorTextSecondary</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Subtitle2
        </item>
    </style>

    <!-- Style for the image of the observatory -->
    <style name="ObservatoryImageButtonStyle">
        <item name="android:layout_height">@dimen/image_dimensions</item>
        <item name="android:layout_width">@dimen/image_dimensions</item>
    </style>

    <!-- Style for the MapFragment -->
    <style name="MapFragmentStyle">
        <item name="android:layout_height">@dimen/map_fragment_height</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <!-- TextView for the app widget -->
    <style name="WidgetTextViewStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Subtitle2
        </item>
        <item name="android:textColor">@color/colorTextIcons</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>
</resources>